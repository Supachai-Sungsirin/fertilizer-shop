@{
    ViewData["Title"] = "Home";
}

<div class="text-center mt-5">
    <h1>@ViewBag.WelcomeMessage</h1>
    <p class="lead">ระบบจัดการร้านขายปุ๋ยครบวงจร สำหรับเจ้าของร้าน ผู้จัดการ และแคชเชียร์</p>

    <div class="mt-4">
        <a class="btn btn-success btn-lg me-2" href="@Url.Action("Login","Account")">Login</a>
        <a class="btn btn-outline-success btn-lg" href="@Url.Action("Register","Account")">Register</a>
    </div>
</div>